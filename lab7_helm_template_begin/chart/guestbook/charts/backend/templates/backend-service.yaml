apiVersion: v1
kind: Service
metadata:
  labels:
    name: {{ .Release.Name}}-{{ .Chart.Name}}
  name: {{ .Release.Name}}-{{ .Chart.Name}}
spec:
  ports:
    - protocol: "TCP"
      port: 80
      targetPort: 3000
  selector:
    app: {{ .Release.Name}}-{{ .Chart.Name}}
 # type: NodePort
